import { Jogador } from './Jogador.js';

class Sabotador extends Jogador {
    constructor(nome, posicao) {
        super(nome, posicao);
    }

    sabotagem(posicao) {
    }

    matar(jogador) {
    }
}